version: 0.2

phases:
  install:
    runtime-versions:
      java: corretto17

  build:
    commands:
      - echo "Building vivekapp..."
      - mvn clean package

artifacts:
  files:
    - target/vivekapp.war
    #- appspec.yml
    - scripts/**/*
